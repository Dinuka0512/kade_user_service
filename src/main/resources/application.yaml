server:
  port: 8001

spring:
  application:
    name: user_service
  datasource:
    url: jdbc:mysql://localhost:3306/kade_users
    username: root
    password: 0512
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
